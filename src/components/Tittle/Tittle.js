import React from 'react';
import { Text, View } from 'react-native';
import {TittleStyle} from './Style';

export default function Tittle() {
    return(
        <TittleStyle>ToDo List</TittleStyle>
    )
}